{
	"name": "@cellix/api-core",
	"version": "1.0.0",
	"private": true,
	"description": "Azure Functions application bootstrap for Cellix API applications",
	"type": "module",
	"files": [
		"dist"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"format": "biome format --write",
		"format:check": "biome format .",
		"prebuild": "pnpm run lint",
		"build": "tsc --build",
		"watch": "tsc --watch",
		"test": "vitest run --silent --reporter=dot",
		"test:coverage": "vitest run --coverage --silent --reporter=dot",
		"test:watch": "vitest",
		"lint": "biome lint",
		"clean": "rimraf dist",
		"typecheck": "tsc --noEmit --pretty false -p tsconfig.json"
	},
	"dependencies": {
		"@azure/functions": "catalog:",
		"@cellix/api-services-spec": "workspace:*",
		"@opentelemetry/api": "1.9.0"
	},
	"devDependencies": {
		"@cellix/config-typescript": "workspace:*",
		"@cellix/config-vitest": "workspace:*",
		"@vitest/coverage-istanbul": "catalog:",
		"rimraf": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:",
		"@types/node": "catalog:"
	}
}
